[build]
  base = ""
  command = "pip install -r requirements.txt && python build_static_site.py"
  publish = "dist"

[build.environment]
  PYTHON_VERSION = "3.11"
  
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
